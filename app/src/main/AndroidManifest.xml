<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.navadhan">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"
        android:usesCleartextTraffic="true">
        <activity android:name=".GetHeatmapActivity"></activity>
        <activity android:name=".ListFamilyMemberActivity" />
        <activity android:name=".AaddharKycActivity" />
        <activity android:name=".CameraOCRActivity" />
        <activity android:name=".ImageToTextActivity" />
        <activity android:name=".DocumentOCR" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".GetSummaryActivity" />
        <activity android:name=".GetObligationActivity" />
        <activity android:name=".SelectOptionsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>